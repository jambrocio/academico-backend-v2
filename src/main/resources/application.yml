spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/academico_db
    username: carrito_user
    password: carrito_pass
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
    show-sql: true

server:
  port: 8080
